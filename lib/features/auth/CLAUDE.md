# Features Scoped Guidelines

- Every feature folder must follow: `data`, `domain`, `presentation`.
- Features must be self-contained and decoupled from sibling features.
- Cross-feature communication must happen via domain interfaces or shared core services.